{
  "name": "firebase-github-auth",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/registration-ranges.test.js tests/registration-ui.test.js",
    "test:rules": "firebase emulators:exec --config firebase.test.json --only firestore --project demo-student-marks \"node --test tests/firestore.rules.test.js\""
  },
  "dependencies": {
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase-tools": "15.29.0",
    "happy-dom": "20.14.0"
  }
}
